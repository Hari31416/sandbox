# Nexus Sandbox service — local development
#
# Use microsandbox (hardware-isolated microVMs). Requires `msb` CLI:
#   msb --version

SANDBOX_DEFAULT_BACKEND=microsandbox
SANDBOX_DEFAULT_IMAGE=python:3.12
SANDBOX_HOST=127.0.0.1
SANDBOX_PORT=8787
